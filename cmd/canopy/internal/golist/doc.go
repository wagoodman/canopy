// Package golist provides Go package discovery functionality by wrapping the
// `go list` command. It enables querying package metadata, filtering packages,
// and selecting test targets from the workspace.
package golist
